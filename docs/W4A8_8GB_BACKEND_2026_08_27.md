# H3 W4A8 8GB隔离后端

## 发布边界

控制台提供`fl2va_w4a8_8gb`与`ref2va_w4a8_8gb`两个启动器。两者固定到8GB资源
后端，不能借用16/24GB预算，并使用与INT8
版本相同的H3网络拓扑、Qwen、Video/Audio VAE、V24调度器和用户两控制量；变化的是
DiT基础权重的数值格式与显存执行图：

- 200个主要线性层使用group-16 packed W4、FP8 scale与运行时INT8 activation；
- 非目标张量保持checkpoint声明的原始精度；
- 首遍生成最高720P×15秒；
- 720P×15秒的Ref2VA发布边界为一个参考素材；更多参考按真实packed-token预算拒绝；
- H3二次采样开放720P/1080P、1–15秒；1440P仍拒绝；
- Base/LoRA仍是任务级热开关，不是第五、第六份基础权重。

该权重是有损量化，不能声称与INT8画质等价。当前门证明执行、显存与接口合同；
W4A8相对INT8的主观质量仍以Human成片审核为准。

## LoRA兼容结论

现有`minimax_h3_turbo_v4_step600_ema.safetensors`无需为W4A8重新下载一份。其metadata
声明`base_model=MiniMax-H3`，518个tensor组成259对LoRA A/B，目标名称覆盖同一50层
H3拓扑。运行时在W4基础投影之后以独立低秩分支叠加delta，不把LoRA烘焙回4-bit
权重，因此：

- INT8与W4A8共用同一个LoRA文件；
- FL2VA与Ref2VA沿用已经发布的任务级LoRA开关；
- W4量化误差与LoRA蒸馏误差会共同存在，技术兼容不等于质量完全相同。

实际CUDA门已验证W4完整投影、三个QKV output slice以及LoRA开/关的切片结果与对应
完整计算一致。FL2VA和Ref2VA的W4+LoRA均完成了真实H264/AAC成片。

## 低显存执行机制

8GB后端的内部优化器不按提示词内容写场景分支。它使用真实width/height/frames、文本token、
参考条件token和packed-token总数求解最快可行图。8GB路线的主要约束为：

- compact full-context K/V；
- Query与输出投影chunk 4096；
- W4 MLP按2048 token切块，仍执行完整`fc1 → SwiGLU → fc2`；
- 长序列必要时使用一个W4 block device buffer；
- Video VAE按阶段独占GPU，并保留288空间tile与精确host temporal输出；
- 1080P二采按真实长度自动选择全画布或相位对齐的时间窗；15秒16:9使用4个
  全空间窗口，避免空间接缝，重叠计算比例约1.141；
- 硬8GB最终Video VAE解码将decoder尾部12个block逐块换入，并以8个时间tile
  为一批写入CPU输出，既守住显存上限，又避免逐tile调度造成的巨大速度损失；
- Ref2VA参考图/首尾帧编码保持FP16 VAE权重，MiniMax后验moment、采样与latent归一化
  仍保留FP32边界，避免旧路径把整套5.2GB VAE临时膨胀到FP32。

W4 output slicing与MLP chunk只重排同一量化算子和LoRA计算，不删层、不删token。
V24的Actual/Forecast和逐层Attention近似仍由用户的步数与加速力度独立决定。

## 物理验收

环境为RTX 4090 SM89、PyTorch 2.13+cu130、CUDA Toolkit 13.3。为了模拟8GB设备可供
PyTorch使用的空间，测试进程设置7.25GiB硬分配上限；这比在24GB卡上仅观察低占用更
严格，但最终仍建议在目标8GB SM89显卡上做一次安装烟测。

| 启动器/变体 | 任务 | 步数/加速 | 热任务时间 | 峰值allocated/reserved | 结果 |
|---|---|---:|---:|---:|---|
| FL2VA W4 Base | 1280×736，362帧 | 5 / 95 | 121.458秒 | 6.9793 / 7.0879GiB | 成功 |
| FL2VA W4 LoRA | 864×480，124帧 | 4 / 75 | 22.111秒 | 6.7421 / 6.8086GiB | 成功 |
| Ref2VA W4 Base | 864×480，124帧，1图 | 5 / 95 | 70.850秒 | 6.7440 / 6.8125GiB | 成功 |
| Ref2VA W4 LoRA | 864×480，124帧，1图 | 4 / 95 | 20.916秒 | 6.7421 / 6.8086GiB | 成功 |
| FL2VA W4 Base二采 | 480P→1920×1088，362帧 | 1 / 95 | 216.938秒 | 6.9927 / 7.1230GiB | 成功 |
| Ref2VA W4 Base二采 | 480P→1920×1088，362帧，1图 | 1 / 95 | 235.000秒 | 6.9927 / 7.1230GiB | 成功 |
| Ref2VA W4 Base二采 | 480P→1920×1088，124帧，1图 | 1 / 95 | 76.772秒 | 6.8906 / 7.0254GiB | 成功 |
| Ref2VA W4 Base二采 | 480P→1920×1088，124帧，1音频 | 1 / 95 | 74.061秒 | 6.8906 / 7.0254GiB | 成功 |

FL2VA 720P×15秒运行中NVML观测峰值约7460MiB；DiT阶段GPU约409–424W，Video VAE
阶段约479–482W。四个输出均包含H264视频与32kHz双声道AAC音频。

可供Human直接查看的成片：

- `output/8a16440c-abd1-48ac-b3a8-bfd739c01f4d.mp4`
- `output/c693c582-27e2-4e28-9fa3-282b75811567.mp4`
- `output/369d5a6b-ae1e-412a-844e-3c0f458859d5.mp4`
- `output/6942d273-6fe7-42c2-ad99-35d3f2079a04.mp4`

这些时间不能直接拿来比较Base与LoRA质量/速度：步数、加速档位、Qwen缓存状态和
输入协议并不全部相同。它们是有界的能力门，不是跨路线benchmark。

同一Ref2VA热会话连续两次1080P×5秒二采分别为76.243秒和70.113秒，第二单没有
显存碎片回归，峰值仍为6.8906/7.0254GiB。真实取消门在提交取消后2.95秒确认，
没有留下半成品；后端停止后Torch allocated/reserved为0.0226/0.0605GiB。完整报告位于
`/root/x-minimaxh3-runtime/runtime/validation/w4a8_8gb_1080p*`，静态产品矩阵见
`/root/x-minimaxh3-runtime/runtime/validation/w4a8_8gb_1080p_envelope_20260828/`。

## 权重与复现

两份W4A8权重固定到`starsfriday/MiniMax-H3-w4a8`的revision
`2a5ad5871296af3506fda7c5edd571612253cedb`。发行manifest保存两份文件的精确字节数和
SHA-256；`scripts/download_models.py`会安装到`models/diffusion_models/`并执行完整
哈希验证。该转换是独立的实验性权重，仍受MiniMax H3 Community License约束。
